#include<stdio.h>
int main ()
{ 
    int a=7, b=5;
    int pm=2*(a+b);
    int ar=a*b;
    printf("Perimeter of the rectangle = %d inches\n", pm);
    printf("Area of rectangle is = %d square inches\n", ar);
    return 0;
}